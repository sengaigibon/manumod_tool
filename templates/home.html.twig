{% extends 'base.html.twig' %}

{% block body %}
    {% if is_granted('ROLE_USER') %}
        <div class="links">
            <a href="{{ path('dashboard_index') }}">Dashboard</a>
        </div>
    {% endif %}
{% endblock %}