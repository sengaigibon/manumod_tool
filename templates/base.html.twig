<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>

    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,600" rel="stylesheet">

    <!-- Styles -->
    <style>
        html, body {
            background-color: #fff;
            color: #636b6f;
            font-family: 'Nunito', sans-serif;
            font-weight: 200;
            height: 100vh;
            margin: 0;
        }
        .top-right {
            position: absolute;
            right: 10px;
            top: 18px;
        }
        .top-left-logo-dashboard {
            position: absolute;
            left: 26px;
            top: 10px;
        }
        .full-height {
            height: 70vh;
        }
        .flex-center {
            align-items: center;
            display: flex;
            justify-content: center;
        }
        .position-ref {
            position: relative;
        }
        .content {
            text-align: center;
        }
        .title {
            font-size: 70px;
        }
        .links > a {
            color: #636b6f;
            padding: 0 25px;
            font-size: 13px;
            font-weight: 600;
            letter-spacing: .1rem;
            text-decoration: none;
            text-transform: uppercase;
        }
        .m-b-md {
            margin-bottom: 30px;
        }
    </style>
</head>
<body>
<div class="flex-center position-ref full-height">
    <div class="top-left-logo-dashboard">
        <img src="./assets/img/modix-logo.png" style="width: 140px;">
    </div>
    <div class="top-right links">
        {% if is_granted('ROLE_USER') or is_granted('ROLE_ADMIN') %}
            <p>
                Logged in as {{ app.user.email }}<br>
                <a href="{{ path('app_logout') }}">Logout</a>
            </p>
        {% else %}
            <a href="{{ path('app_login') }}">Log in with Okta</a>
        {% endif %}
    </div>

    <div class="content">
        <div class="title m-b-md">
            ManuMod Selfservice Tool
        </div>
        {% block body %}
        {% endblock %}
    </div>
</div>

{% block javascripts %}{% endblock %}
</body>
</html>